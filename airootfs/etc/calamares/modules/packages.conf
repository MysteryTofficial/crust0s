# Crust OS — Calamares package installer (fallback/network)
# This is only used if you enable the packages module in the sequence.
---
backend: pacman
operations:
  - install:
      - linux-firmware
      - linux-lts
      - linux-lts-headers
